name: CI

on: [push]

jobs:

    build:

        runs-on: ubuntu-latest

        steps:
            - uses: actions/checkout@v1
            - name: "### Build script ###"
              run: yarn install && yarn tsc
            - name: "### Run test ###"
              run: yarn test
